

int main() {
    int num, a12b_34, 13aaa;
    string x = "un string \
                pe mai \" multe linii "
    printf("Enter an integer: ");
    scanf("%d", &num);

    // true if num is perfectly divisible by 2
    if(num % 2 == 0)
        printf("%d is even.", num);
    else
        printf("%d is odd.", num);
    
    return 0;    /* comentariu ** pe /mai
                   multe linii  */   num++;
}